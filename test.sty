% This file must be saved with UTF-8 encoding, otherwise pandoc will complain
% for instance about nordic characters.
\usepackage[utf8]{inputenc}
\usepackage{times}
\usepackage{blindtext}
\usepackage{graphicx}
\usepackage{textcase}
\usepackage[explicit]{titlesec}
\usepackage{geometry}
%Margins Formatting
\geometry{left=1.5in, right=1in, bottom=1.25in, top=1in}

\usepackage{setspace}
\setstretch{1.5}

%Table of Contents Formatting
\usepackage{tocloft}
\renewcommand{\cftpartleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cftpartleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\tocloftpagestyle{fancy}
%%Renaming Sections
\renewcommand{\contentsname}{TABLE OF CONTENTS}
\renewcommand{\listfigurename}{LIST OF FIGURES}
\renewcommand{\listtablename}{LIST OF TABLES}
%%Formatting the different titles to be not bold, smaller, and have the necessary page and Figure/Table labels
\renewcommand{\cftsecfont}{\normalfont }
\renewcommand{\cftsecpagefont}{\normalfont}
\renewcommand{\cftlottitlefont}{\hfill \normalsize}
\renewcommand{\cftafterlottitle}{\hfill \mbox{} \\[\baselineskip]\mbox{}Table\hfill{\normalfont Page}}
\renewcommand{\cftloftitlefont}{\hfill \normalsize}
\renewcommand{\cftafterloftitle}{\hfill \mbox{} \\[\baselineskip]\mbox{}Figure\hfill{\normalfont Page}}
\renewcommand{\cfttoctitlefont}{\hfill \normalsize}
\renewcommand{\cftaftertoctitle}{\hfill \mbox{} \\[\baselineskip]\mbox{}\hfill{\normalfont Page}}

%Making List of Schemes
\usepackage{newfloat}
\DeclareFloatingEnvironment[
  fileext   = los ,    % name of file extension
  listname  = {LIST OF SCHEMES},
  name      = Scheme , % name of float type
  within    = section ,% counter within
  placement = htbp     % default placement
]{scheme}


%Header Formatting
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.0pt}
\renewcommand{\footrulewidth}{0.0pt}
\rhead{\thepage}
\cfoot{}

%%Formatting the Chapter heading so individual documents added as children to the main document
%%behave as necessary
%%Formatting the chapter heading, RMarkdown uses new pages as sections though, so there's that
 \titleformat
{\section} % command
[display] % shape
{\normalsize}
{} % label
{0ex} % sep
{
\centering
\vspace{0.5in}
\MakeTextUppercase{Chapter \thesection : #1}
}%before code
[]%aftercode

%%Formatting the subsection heading 
 \titleformat
{\subsection} % command
[display] % shape
{\normalsize}
{} % label
{0ex} % sep
{
\centering
\thesubsection \hspace{1mm}
\underline{#1}
}%before code
[]%aftercode


%%Formatting the subsubsection heading 
 \titleformat
{\subsubsection} % command
[display] % shape
{\normalsize}
{} % label
{0ex} % sep
{
\centering
\thesubsubsection \hspace{1mm}
#1
}%before code
[]%aftercode
